[project]
name = "lerobot_camera_zed"
version = "0.1.0"
description = "LeRobot camera plugin for Stereolabs ZED cameras."
requires-python = ">=3.10"

[tool.setuptools]
packages = ["lerobot_camera_zed"]
